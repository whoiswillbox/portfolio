/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/scroll-area`. See docs/component-customizations.md. */
export { ScrollArea, ScrollBar } from "@/components/cardboard/scroll-area"
