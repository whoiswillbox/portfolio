/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/label`. See docs/component-customizations.md. */
export { Label } from "@/components/cardboard/label"
