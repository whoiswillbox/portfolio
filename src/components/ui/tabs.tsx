/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/tabs`. See docs/component-customizations.md. */
export { Tabs, TabsList, TabsTrigger, TabsContent, tabsListVariants } from "@/components/cardboard/tabs"
