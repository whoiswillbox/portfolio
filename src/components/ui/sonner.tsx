/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/sonner`. See docs/component-customizations.md. */
export { Toaster } from "@/components/cardboard/sonner"
