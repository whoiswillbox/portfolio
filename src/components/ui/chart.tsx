/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/chart`. See docs/component-customizations.md. */
export {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
  ChartLegend,
  ChartLegendContent,
  ChartStyle,
} from "@/components/cardboard/chart"
export type { ChartConfig } from "@/components/cardboard/chart"
