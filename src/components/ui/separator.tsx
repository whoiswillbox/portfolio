/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/separator`. See docs/component-customizations.md. */
export { Separator } from "@/components/cardboard/separator"
