/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/calendar`. See docs/component-customizations.md. */
export { Calendar, CalendarDayButton } from "@/components/cardboard/calendar"
