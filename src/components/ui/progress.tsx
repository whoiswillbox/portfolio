/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/progress`. See docs/component-customizations.md. */
export { Progress } from "@/components/cardboard/progress"
