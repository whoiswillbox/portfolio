/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/native-select`. See docs/component-customizations.md. */
export {
  NativeSelect,
  NativeSelectOptGroup,
  NativeSelectOption,
} from "@/components/cardboard/native-select"
