/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/resizable`. See docs/component-customizations.md. */
export {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@/components/cardboard/resizable"
