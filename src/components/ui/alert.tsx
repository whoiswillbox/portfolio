/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/alert`. Migrate imports to cardboard/ over time.
   See docs/component-customizations.md. */
export { Alert, AlertTitle, AlertDescription, AlertAction } from "@/components/cardboard/alert"
