/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/tooltip`. Migrate imports to cardboard/ over time.
   See docs/component-customizations.md. */
export { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/cardboard/tooltip"
