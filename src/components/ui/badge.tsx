/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/badge`. Migrate imports to cardboard/ over time.
   See docs/component-customizations.md. */
export { Badge, badgeVariants } from "@/components/cardboard/badge"
