/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/checkbox`. See docs/component-customizations.md. */
export { Checkbox } from "@/components/cardboard/checkbox"
