/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/slider`. See docs/component-customizations.md. */
export { Slider } from "@/components/cardboard/slider"
