/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/skeleton`. See docs/component-customizations.md. */
export { Skeleton } from "@/components/cardboard/skeleton"
