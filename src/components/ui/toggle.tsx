/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/toggle`. See docs/component-customizations.md. */
export { Toggle, toggleVariants } from "@/components/cardboard/toggle"
