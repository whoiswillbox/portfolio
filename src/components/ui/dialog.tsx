/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/dialog`. See docs/component-customizations.md. */
export {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogOverlay,
  DialogPortal,
  DialogTitle,
  DialogTrigger,
} from "@/components/cardboard/dialog"
