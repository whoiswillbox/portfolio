/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/input`. See docs/component-customizations.md. */
export { Input } from "@/components/cardboard/input"
