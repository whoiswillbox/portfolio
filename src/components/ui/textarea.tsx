/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/textarea`. See docs/component-customizations.md. */
export { Textarea } from "@/components/cardboard/textarea"
