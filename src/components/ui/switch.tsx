/* Forked into Cardboard. Re-export shim — the owned component lives at
   `@/components/cardboard/switch`. See docs/component-customizations.md. */
export { Switch } from "@/components/cardboard/switch"
